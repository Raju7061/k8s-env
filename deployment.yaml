apiVersion: apps/v1
kind: Deployment
metadata:
  name: raju-app
  namespace: flux-system
  labels:
    app: raju-app
spec:
  replicas: 2  # Number of instances
  selector:
    matchLabels:
      app: raju-app
  template:
    metadata:
      labels:
        app: raju-app
    spec:
      containers:
        - name: raju-app
          image: 2024dock/raju-app:25.3.1  # Using your Docker image
          ports:
            - containerPort: 3000
